void main(List<String> arguments) {
  var altura = 1.80;
  var peso = 105;

  var result = peso / (altura * altura);
  //String resultado = result.toStringAsFixed(2);

  print(result.toStringAsFixed(2));
}
